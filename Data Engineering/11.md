eg i  have written a code 
so i will initialize the spark context.

        sc = sparkContext("Local" "RDD")
        rdd1 = sc.parallize([1,2,3,4])
        rdd2 = sc.textfile("path")

now i wanted to do the transformation on this particualr data..
i have to filter the value

        filtered = rdd1.filter(lambda x: x>2)
        double = filtered.map(lambda x: x>2)
        result = double.collect()


some of the action commands
this triggers the flow to work.
collect => it will return all the elements
count() => 
take(n) => display top n values
reduce() =>


then we have transformation which does not trigger
1. map()
2. filter()
3. group by ()
4. joins ()



there was a text file .. we filtered it.. we mapped it .. we reduced it . and then finally we have to do collect.

here is when the action happens in collect..
and before that it is transformation

now we will do coding in jupyterlab


